#!/bin/bash

echo "OK"

basedir="`dirname $0`/../../"

echo $basedir

export GEM_HOME="`(cd "$basedir/build/gems"; pwd)`"
export GEM_PATH=""


exec java $JAVA_OPTS -cp "$basedir/build/*:$basedir/common/lib/*" org.jruby.Main --1.9 --debug $basedir/build/gems/bin/rspec "$@"
